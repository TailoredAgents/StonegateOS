
import { NextResponse, type NextRequest } from "next/server";

type JunkQuoteRequest = {
  source?: string;
  contact: {
    name: string;
    phone: string;
    timeframe: "today" | "tomorrow" | "this_week" | "flexible";
  };
  job: {
    types: string[];
    perceivedSize: string;
    notes?: string | null;
    zip: string;
    photoUrls: string[];
  };
};

export async function POST(request: NextRequest) {
  // TODO: implement real handler; placeholder to avoid 404s during refactor.
  return NextResponse.json({ error: "not_implemented" }, { status: 501 });
}
