import { PartnerAccessHelp } from "./PartnerAccessHelp";

/** Kept for old imports; new access is arranged by Stonegate, never a signup form. */
export function PartnerAccessRequestForm(_props: {
  initialError?: string | null;
}) {
  return <PartnerAccessHelp />;
}
