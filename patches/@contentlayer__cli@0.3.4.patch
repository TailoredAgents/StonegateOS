diff --git a/dist/commands/BuildCommand.js b/dist/commands/BuildCommand.js
index 17ef19082c821de94e06dc468dbff62217b272cd..fdf7ceaf0ffb375a5c3730a5ce10ff02c1cec5be 100644
--- a/dist/commands/BuildCommand.js
+++ b/dist/commands/BuildCommand.js
@@ -4,7 +4,7 @@ import { BaseCommand } from './_BaseCommand.js';
 export class BuildCommand extends BaseCommand {
     constructor() {
         super(...arguments);
-        this.executeSafe = () => pipe(this.clearCacheIfNeeded(), T.chain(() => core.getConfig({ configPath: this.configPath })), T.tap((config) => (config.source.options.disableImportAliasWarning ? T.unit : T.fork(core.validateTsconfig))), T.chain((config) => core.generateDotpkg({ config, verbose: this.verbose })), T.tap(core.logGenerateInfo), OT.withSpan('@contentlayer/cli/commands/BuildCommand:executeSafe'));
+        this.executeSafe = () => pipe(this.clearCacheIfNeeded(), T.chain(() => core.getConfig({ configPath: this.configPath })), T.tap((config) => (config.source.options.disableImportAliasWarning ? T.unit : T.fork(core.validateTsconfig))), T.chain((config) => core.generateDotpkg({ config, verbose: this.verbose })), T.tap(core.logGenerateInfo), T.asUnit, OT.withSpan('@contentlayer/cli/commands/BuildCommand:executeSafe'));
     }
 }
 BuildCommand.paths = [['build']];
diff --git a/src/commands/BuildCommand.ts b/src/commands/BuildCommand.ts
index 4d96bc33bc11e85ba68d87a920183d0f3c0df95a..7485dd55dc0989892905cee13dc6f5739402d3be 100644
--- a/src/commands/BuildCommand.ts
+++ b/src/commands/BuildCommand.ts
@@ -25,6 +25,11 @@ export class BuildCommand extends BaseCommand {
       T.tap((config) => (config.source.options.disableImportAliasWarning ? T.unit : T.fork(core.validateTsconfig))),
       T.chain((config) => core.generateDotpkg({ config, verbose: this.verbose })),
       T.tap(core.logGenerateInfo),
+      // Clipanion command handlers must resolve to an integer exit code or
+      // undefined. generateDotpkg returns its generation result, which causes
+      // Cli.runExit to assign an object to process.exitCode on successful
+      // builds under current Node releases.
+      T.asUnit,
       OT.withSpan('@contentlayer/cli/commands/BuildCommand:executeSafe'),
     )
 }
